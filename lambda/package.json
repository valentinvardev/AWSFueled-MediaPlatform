{
  "name": "mediaseller-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "bundle": "npm run build && cd dist && zip -r ../function.zip .",
    "deploy": "npm run bundle && aws lambda update-function-code --function-name mediaseller-photo-processor --zip-file fileb://../function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "prisma": "^6.6.0",
    "typescript": "^5.0.0"
  }
}
